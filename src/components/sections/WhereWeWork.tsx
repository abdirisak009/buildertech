"use client";
import { MapPin } from "lucide-react";
import { Container } from "@/components/ui/Container";
import { Reveal } from "@/components/motion/Reveal";
import { SERVICE_CITIES_ALL,SERVICE_COUNTIES } from "@/content/georgiaMap";
type Copy={eyebrow:string;title:string;lead:string;countiesLabel:string;citiesLabel:string};
function RotatingLine({items,label}:{items:string[];label:string}){const line=[...items,...items];return <div className="overflow-hidden border-y border-white/10 bg-white/[.03] py-4"><div className="mb-3 flex items-center gap-2 px-5 text-xs font-bold uppercase tracking-[.18em] text-gold-400"><MapPin className="size-4"/>{label}</div><div className="ww-marquee flex w-max gap-3 px-5">{line.map((item,i)=><span key={`${item}-${i}`} className="whitespace-nowrap rounded-full border border-white/10 bg-white/[.05] px-5 py-2.5 text-sm font-semibold text-navy-50">{item}</span>)}</div></div>}
export function WhereWeWork({copy}:{copy:Copy}){return <section className="relative isolate overflow-hidden bg-navy-950 pt-20 pb-12 text-white sm:pt-28 sm:pb-14"><div aria-hidden className="absolute inset-0 bg-blueprint opacity-[.07]"/><Container className="relative"><div className="mx-auto max-w-3xl text-center"><Reveal><p className="text-xs font-bold uppercase tracking-[.22em] text-gold-400">{copy.eyebrow}</p><h2 className="mt-5 text-display-lg text-white">{copy.title}</h2><p className="mt-6 text-lg leading-8 text-navy-100">{copy.lead}</p></Reveal></div><Reveal delay={.12}><div className="mx-auto mt-12 max-w-5xl overflow-hidden rounded-3xl border border-white/10 bg-black shadow-2xl"><video src="/where-we-work.mp4" poster="/images/5387261.jpg" autoPlay muted loop playsInline controls className="aspect-video w-full object-cover"><track kind="captions"/></video></div></Reveal><div className="mx-auto mt-12 max-w-6xl space-y-5"><RotatingLine label={copy.countiesLabel} items={SERVICE_COUNTIES}/><RotatingLine label={copy.citiesLabel} items={SERVICE_CITIES_ALL}/></div></Container></section>}
